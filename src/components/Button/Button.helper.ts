import React from 'react';
import { ButtonProps } from './Button.types';

export const cssImplicator = (props: ButtonProps): React.CSSProperties => {
  // to-do!
  const todo = props;
  return {};
};
